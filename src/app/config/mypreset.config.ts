import Lara from '@primeng/themes/lara';
import { ThemeType } from 'primeng/config';

export const MyPreset: ThemeType = {
  preset: Lara,
  options: {
    //inputVariant: 'filled',
    darkModeSelector: '.my-app-dark'
  }
}